<h1>Post an Event</h1>
<%= form_tag do %>
  Form contents
<% end %>

<h3><%= @post.title %></h3>
<p><%= @post.body %></p>
<%= link_to "edit", edit_event_post_path(@post) if @post.user_id == current_user.id %>
<%= link_to "delete", event_post_path(@post), :method => :delete if @event.user_id == current_user.id %>
